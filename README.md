# Research Agent

This is a Python-based AI agent that fetches news and analyzes it using OpenRouter. It runs daily via GitHub Actions and generates JSON reports stored in the `/data` directory.

## Project Goals
- To analyze recent news articles effectively.
- To create stable and reliable automated runs.